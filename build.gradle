apply plugin: 'java'
apply plugin: 'application'

apply from: 'gradle/integration.gradle'

mainClassName = 'Driver'
applicationName = 'agora'
version = '0.0.' + System.getProperty('PIPELINE_VERSION', '0');

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}
